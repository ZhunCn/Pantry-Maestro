const Inventory = require('./Inventory');
const Item = require('./Item');
const Invite = require('./Invite');
const Workspace = require('./Workspace');
const User = require('./User');

module.exports = {Inventory, Item, Invite, Workspace, User};
